# 🤖 SourceMate — AI-бот для поиска академических источников

SourceMate — это Telegram-бот, который помогает студентам, исследователям и авторам находить **релевантные научные источники** (статьи, журналы, сборники, конференции и т.д.) по любой теме.  
Бот использует **LLM (Ollama / локальный ИИ)** для интеллектуального анализа запросов и фильтрации нерелевантных материалов.

---

## 🚀 Возможности

- 🔍 Ищет источники по запросу пользователя  
- 🧠 Анализирует смысл темы и подбирает максимально точные совпадения  
- 📚 Источники из:
  - Crossref (научные журналы)
  - Semantic Scholar
  - Wikipedia
  - НЭБ (rusneb.ru, при подключении)
- ⚙️ Использует локальный AI (Ollama) для оценки релевантности  
- 💬 Работает через Telegram-бота с удобной **пагинацией**  
- ⚡️ Безопасно — `.env` исключён из репозитория  

---

## 🧩 Структура проекта

```
source search bot/
├── src/
│   ├── core/
│   │   ├── aggregate.js         # Основная логика поиска
│   │   ├── ranking.js           # Оценка релевантности
│   │   ├── filters.js           # Фильтрация нерелевантных источников
│   │   ├── domain_profile.js    # Семантический профиль темы
│   │   └── lexical_relevance.js # Лексическая релевантность
│   │
│   ├── sources/                 # Источники данных
│   │   ├── crossref.js
│   │   ├── semanticscholar.js
│   │   ├── wikipedia.js
│   │   └── rusneb.js (опционально)
│   │
│   ├── llm/
│   │   └── llm.js               # Работа с локальной LLM (Ollama)
│   │
│   ├── transport/
│   │   └── telegram/
│   │       ├── bot.js           # Telegram-бот (Telegraf)
│   │       └── pagination.js    # Постраничный вывод
│   │
│   ├── config.js                # Настройки проекта
│   └── utils.js                 # Общие утилиты
│
├── .gitignore
├── package.json
└── .env.example
```

---

## ⚙️ Установка и запуск

### 1️⃣ Клонирование репозитория
```bash
git clone https://github.com/ivlieva006/SoureMate.git
cd SoureMate
```

### 2️⃣ Установка зависимостей
```bash
npm install
```

### 3️⃣ Настройка `.env`
Создай файл `.env` (или скопируй из `.env.example`) и добавь туда:
```bash
BOT_TOKEN=токен_от_Telegram
LLM_ENABLED=true
LLM_BASE_URL=http://localhost:11434/v1
LLM_MODEL=llama3.1
```

> 💡 Ollama можно установить с сайта [ollama.ai](https://ollama.ai/)  
> Затем запустить модель:
> ```bash
> ollama pull llama3.1
> ollama serve
> ```

### 4️⃣ Запуск бота
```bash
npm start
```

---

## 💡 Пример использования

**Пользователь:**  
> "Искусственный интеллект в образовании"

**Бот:**  
> Подбирает источники с краткими описаниями, DOI и ссылками на публикации (Crossref, Semantic Scholar и др.), сортируя их по степени релевантности (0–100).

---

## 🧠 Технологии

- **Node.js (v18+)**
- **Telegraf** — Telegram Bot API
- **Ollama (LLM)** — локальный AI-модуль
- **Crossref / Semantic Scholar API**
- **Wikipedia REST API**
- **p-limit / he / dotenv**

---

